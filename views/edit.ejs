<% include partials/header %>

<header id="header">
    <h1> -- Welcome, Hero! -- </h1>
</header>

<div class="flex-container">
    <% for(let i = 0; i < result.length; i++){
        if(i == 0 || result[i].name != result[i-1].name){ %> <!-- loop through and display skills-->
        <div class="skill">
            <div class="skill-progress">
                <div class="progressbar">
                    <span class="progress green" style="width:<%= result[i].curr_xp %>% 
                        <% if(result[i].curr_xp == 100){ %>
                            ;border-radius:5px"
                        <% } else{ %>
                            "
                        <% } %> <!-- display progress -->
                    </span>
                </div>
                <span class="edit-label"><%= result[i].name %></span>
                <span class="lvl-progress">LVL <%= result[i].curr_lvl %>/<%= result[i].max_lvl %></span>
                <form action="/profile/editSkills" method="POST">
                    <input type="hidden" value="<%= result[i].skill_ID %>" name="id">
                    <input type="submit" class="edit btn" value="Edit">
                </form>
                <a href="/profile/edit/deleteSkill/<%= result[i].skill_ID%>" class="delete btn">Delete</a>
            </div>
            <div class="battles">
                <% if(result[i].battle_ID != null){
                    for(let j = 0; j < result.length; j++){
                    if(result[j].name == result[i].name){ %>
                        <div class="battle">
                            <form action="/profile/editBattles" method="POST">
                                <input type="hidden" value="<%= result[j].battle_ID %>" name="battleID">
                                <input type="submit" class="edit btn small" value="Edit">
                            </form>
                            <a href="/profile/edit/deleteBattle/<%= result[j].battle_ID%>" class="delete btn small">Del</a>
                            <span class="battle-name"><%= result[j].description %></span>
                            <span class="xp"><%= result[j].xp %>XP</span>
                        </div>
                    <% };
                }}; %>
                <form action="/profile/addBattle" method="POST">
                    <input type="hidden" value="<%= result[i].skill_ID %>" name="skillID">
                    <input type="submit" class="add btn small" value="Add">
                </form>
            </div>
        </div>
    <% };
    }; %>
</div>

<% include partials/footer %>